using UnityEngine;

// Phase 8 — Partícula custom 3D (struct, sin MonoBehaviour).
// Misma filosofía que FluidParticle2D: datos puros, sin lógica.
public struct FluidParticle3D
{
    public Vector3 Position;
    public Vector3 Velocity;
    public float   Density;
}
